version: '3.8'

services:
  # Zookeeper for Kafka
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    hostname: zookeeper
    container_name: fraud-zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Kafka broker
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    hostname: kafka
    container_name: fraud-kafka
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: 'zookeeper:2181'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 5

  # Kafka topic initialization
  kafka-init:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint: ['/bin/sh', '-c']
    command: |
      "
      echo 'Creating Kafka topics...'
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic transactions-live --replication-factor 1 --partitions 4
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic fraud-alerts --replication-factor 1 --partitions 2
      kafka-topics --bootstrap-server kafka:29092 --create --if-not-exists --topic transactions-enriched --replication-factor 1 --partitions 4
      echo 'Topics created:'
      kafka-topics --bootstrap-server kafka:29092 --list
      "

  # Redis for enrichment
  redis:
    image: redis:7-alpine
    hostname: redis
    container_name: fraud-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Commander (UI for Redis)
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: fraud-redis-ui
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "8085:8081"
    depends_on:
      redis:
        condition: service_healthy

  # Kafka UI
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: fraud-kafka-ui
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
    depends_on:
      kafka:
        condition: service_healthy

  # Flink JobManager
  flink-jobmanager:
    image: flink:1.18.0-java17
    hostname: flink-jobmanager
    container_name: fraud-flink-jm
    ports:
      - "8081:8081"
    command: jobmanager
    environment:
      - JOB_MANAGER_RPC_ADDRESS=flink-jobmanager
      - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: flink-jobmanager
        parallelism.default: 2
    volumes:
      - flink-checkpoints:/tmp/flink-checkpoints

  # Flink TaskManager
  flink-taskmanager:
    image: flink:1.18.0-java17
    hostname: flink-taskmanager
    depends_on:
      - flink-jobmanager
    command: taskmanager
    deploy:
      replicas: 2
    environment:
      - JOB_MANAGER_RPC_ADDRESS=flink-jobmanager
      - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 2
        parallelism.default: 2
    volumes:
      - flink-checkpoints:/tmp/flink-checkpoints

  # ML Inference Service
  ml-inference:
    build:
      context: ./ml-models/inference
      dockerfile: Dockerfile
    container_name: fraud-ml-inference
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=/app/models
      - MODEL_VERSION=1.0.0
    volumes:
      - ./ml-models/models:/app/models:ro
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - default

  # Phase 1: LNN Service (Liquid Neural Network for irregular time-series)
  lnn-service:
    build:
      context: ./ml-models/lnn-service
      dockerfile: Dockerfile
    container_name: fraud-lnn-service
    ports:
      - "8001:8001"
    environment:
      - MODEL_DIR=/app/models
      - LNN_MODEL_FILE=lnn_fraud_model.pt
      - MODEL_VERSION=1.0.0
    volumes:
      - ./ml-models/models:/app/models:ro
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8001/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - default
    depends_on:
      - ml-inference

  # Phase 1: Causal Inference Service (Explainability)
  causal-service:
    build:
      context: ./ml-models/causal-service
      dockerfile: Dockerfile
    container_name: fraud-causal-service
    ports:
      - "8002:8002"
    environment:
      - MODEL_VERSION=1.0.0
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8002/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - default

  # Phase 2: GNN Service (Graph Neural Network for fraud networks)
  gnn-service:
    build:
      context: ./ml-models/gnn-service
      dockerfile: Dockerfile
    container_name: fraud-gnn-service
    ports:
      - "8003:8003"
    environment:
      - MODEL_DIR=/app/models
      - GNN_MODEL_FILE=gnn_fraud_model.pt
      - MODEL_VERSION=1.0.0
    volumes:
      - ./ml-models/models:/app/models:ro
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8003/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - default
    depends_on:
      - lnn-service

  # Phase 2: NSAI Service (Neuro-Symbolic AI for explainable decisions)
  nsai-service:
    build:
      context: ./ml-models/nsai-service
      dockerfile: Dockerfile
    container_name: fraud-nsai-service
    ports:
      - "8004:8004"
    environment:
      - MODEL_VERSION=1.0.0
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8004/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - default

  # Phase 3: Rule Generation Agent Service (Autonomous rule generation)
  rule-agent-service:
    build:
      context: ./ml-models/rule-agent-service
      dockerfile: Dockerfile
    container_name: fraud-rule-agent-service
    ports:
      - "8005:8005"
    environment:
      - MODEL_VERSION=1.0.0
      - LLM_ENABLED=false
      - LLM_MODEL=gpt-4
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8005/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - default

  # Phase 3: Rule Management Service (Rule lifecycle management)
  rule-management-service:
    build:
      context: ./ml-models/rule-management-service
      dockerfile: Dockerfile
    container_name: fraud-rule-management-service
    ports:
      - "8006:8006"
    environment:
      - MODEL_VERSION=1.0.0
    healthcheck:
      test: ["CMD", "python3", "-c", "import requests; requests.get('http://localhost:8006/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - default

volumes:
  redis-data:
  flink-checkpoints:

networks:
  default:
    name: fraud-network
